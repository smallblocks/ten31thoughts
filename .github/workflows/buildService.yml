name: Build Service

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Prepare StartOS SDK
        uses: Start9Labs/sdk@v1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install newer start-cli
        run: |
          # Download and install newer start-cli with s9pk subcommand
          mkdir -p $HOME/bin
          wget -qO $HOME/bin/start-cli https://github.com/Start9Labs/start-os/releases/download/v0.4.0-alpha.20/start-cli_x86_64-linux
          chmod +x $HOME/bin/start-cli
          echo "$HOME/bin" >> $GITHUB_PATH
          $HOME/bin/start-cli --version

      - name: Build x86_64 package
        run: |
          export PATH="$HOME/bin:$PATH"
          make x86

      - name: Upload .s9pk artifact
        uses: actions/upload-artifact@v4
        with:
          name: tenthoughts.s9pk
          path: tenthoughts*.s9pk
